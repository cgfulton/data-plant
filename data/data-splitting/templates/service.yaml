apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-splitting.fullname" . }}
  labels:
    {{- include "data-splitting.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-splitting.selectorLabels" . | nindent 4 }}
