apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-preparation.fullname" . }}
  labels:
    {{- include "data-preparation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-preparation.selectorLabels" . | nindent 4 }}
