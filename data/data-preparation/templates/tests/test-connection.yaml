apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-preparation.fullname" . }}-test-connection"
  labels:
    {{- include "data-preparation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-preparation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
