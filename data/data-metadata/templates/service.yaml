apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-metadata.fullname" . }}
  labels:
    {{- include "data-metadata.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-metadata.selectorLabels" . | nindent 4 }}
