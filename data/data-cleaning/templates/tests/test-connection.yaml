apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-cleaning.fullname" . }}-test-connection"
  labels:
    {{- include "data-cleaning.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-cleaning.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
