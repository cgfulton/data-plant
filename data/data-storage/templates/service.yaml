apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-storage.fullname" . }}
  labels:
    {{- include "data-storage.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-storage.selectorLabels" . | nindent 4 }}
