apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-storage.fullname" . }}-test-connection"
  labels:
    {{- include "data-storage.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-storage.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
