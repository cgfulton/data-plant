apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-collection.fullname" . }}-test-connection"
  labels:
    {{- include "data-collection.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-collection.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
