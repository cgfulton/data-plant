apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-evaluation.fullname" . }}
  labels:
    {{- include "data-evaluation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-evaluation.selectorLabels" . | nindent 4 }}
