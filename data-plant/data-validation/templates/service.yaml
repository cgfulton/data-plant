apiVersion: v1
kind: Service
metadata:
  name: {{ include "data-validation.fullname" . }}
  labels:
    {{- include "data-validation.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "data-validation.selectorLabels" . | nindent 4 }}
