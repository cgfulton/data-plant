apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-query.fullname" . }}-test-connection"
  labels:
    {{- include "data-query.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-query.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
