apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "data-transformation.fullname" . }}-test-connection"
  labels:
    {{- include "data-transformation.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "data-transformation.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
