apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-jaeger.fullname" . }}-test-connection"
  labels:
    {{- include "operator-jaeger.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-jaeger.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
