apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-maistra.fullname" . }}
  labels:
    {{- include "operator-maistra.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operator-maistra.selectorLabels" . | nindent 4 }}
