apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-mlops.fullname" . }}-test-connection"
  labels:
    {{- include "operator-mlops.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-mlops.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
