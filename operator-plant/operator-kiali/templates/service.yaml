apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-kiali.fullname" . }}
  labels:
    {{- include "operator-kiali.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operator-kiali.selectorLabels" . | nindent 4 }}
