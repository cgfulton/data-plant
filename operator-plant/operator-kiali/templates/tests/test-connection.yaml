apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-kiali.fullname" . }}-test-connection"
  labels:
    {{- include "operator-kiali.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-kiali.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
